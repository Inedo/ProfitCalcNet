name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.x
    - name: DotNet Version
      run: dotnet --info
    - name: Add ProGet NuGet Source
      run: dotnet nuget add source https://proget.inedo.com/nuget/NuGetLibraries/v3/index.json --username api --passsword RichNugetKey
    - name: List Sources
      run: dotnet nuget list source
    - name: Restore dependencies
      run: dotnet restore --source https://proget.inedo.com/nuget/NuGetLibraries/v3/index.json 
    - name: Build
      run: dotnet build --no-restore
    - name: Install PGScan
      run: dotnet tool install pgscan --add-source https://proget.inedo.com/nuget/NuGetLibraries/v3/index.json
    - name: Scan
      run: dotnet tool run pgscan identify --type=nuget --input=ProfiteCalcNet.Console.csproj --project-name=ProfitCalcNetConsole --version=1.0.0 --project-type=application --proget-url=https://proget.inedo.com --api-key=RichNugetKey --consider-project-references
